services:
  - type: web
    name: mathAPI
    env: node
    region: oregon
    plan: free
    repo: https://github.com/<your-org>/mathAPI
    branch: master
    buildCommand: npm ci && npm run build --if-present
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
